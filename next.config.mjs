// @ts-check
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  i18n: {
    locales: ["ja"],
    defaultLocale: "ja",
  },
};

export default nextConfig;
